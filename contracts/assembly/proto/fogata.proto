syntax = "proto3";

package fogata;

import "koinos/options.proto"; 

message amount {
   uint64 koin = 1 [jstype = JS_STRING];
   uint64 vhp = 2 [jstype = JS_STRING];
}

message stake_args {
   bytes account = 1 [(koinos.btype) = ADDRESS];
   amount amount = 2;
}